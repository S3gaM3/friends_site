# Пароль входа в /admin (обязателен)
ADMIN_PASSWORD=changeme-use-a-strong-password

# Секрет подписи сессии (≥16 символов). В production обязателен отдельно от пароля.
ADMIN_SECRET=change-this-long-random-string
